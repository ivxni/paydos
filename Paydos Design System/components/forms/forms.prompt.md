Dark-surface text input with brand focus ring.

```jsx
<Input label="Name" placeholder="Your name" />
<Input label="Guests" type="number" hint="Up to 8 per table" />
```

Renders an upper-case tracked label, an inset field, and a purple glow ring on focus. Accepts all native input props plus `label` and `hint`.
